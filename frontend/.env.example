# Backend API (no trailing slash)
VITE_API_URL=http://localhost:5000/api

# For media URLs (snapshots/videos) — backend origin without /api
VITE_MEDIA_URL=http://localhost:5000

# Socket.IO server (backend origin)
VITE_SOCKET_URL=http://localhost:5000
